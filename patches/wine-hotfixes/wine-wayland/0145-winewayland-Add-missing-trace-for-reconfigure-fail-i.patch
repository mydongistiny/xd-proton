From d1d86ba0b9989093f9953c7a08402bacb21b42b0 Mon Sep 17 00:00:00 2001
From: Etaash Mathamsetty
 <45927311+Etaash-mathamsetty@users.noreply.github.com>
Date: Mon, 1 Jun 2026 23:12:26 -0400
Subject: [PATCH 145/148] winewayland: Add missing trace for reconfigure fail
 in ensure contents.

---
 dlls/winewayland.drv/wayland_surface.c | 4 ++++
 1 file changed, 4 insertions(+)

diff --git a/dlls/winewayland.drv/wayland_surface.c b/dlls/winewayland.drv/wayland_surface.c
index 7b1f6de3acd..dce2272898f 100644
--- a/dlls/winewayland.drv/wayland_surface.c
+++ b/dlls/winewayland.drv/wayland_surface.c
@@ -1864,6 +1864,10 @@ void wayland_surface_ensure_contents(struct wayland_surface *surface,
         wayland_surface_attach_shm(surface, dummy_shm_buffer, damage);
         wl_surface_commit(surface->wl_surface);
     }
+    else
+    {
+        TRACE("Wayland surface not configured yet, not flushing\n");
+    }
 
     if (damage) NtGdiDeleteObjectApp(damage);
 }
-- 
2.54.0

