From 71042a088bf87fe68de838216bc959f01a1c52a2 Mon Sep 17 00:00:00 2001
From: Etaash Mathamsetty
 <45927311+Etaash-mathamsetty@users.noreply.github.com>
Date: Mon, 1 Sep 2025 17:04:15 -0500
Subject: [PATCH 011/113] amdxc: Add support for MLSR-WATERMARK env.

---
 dlls/amdxc64/main.c | 22 +++++++++++++++++++++-
 1 file changed, 21 insertions(+), 1 deletion(-)

diff --git a/dlls/amdxc64/main.c b/dlls/amdxc64/main.c
index 81e42f4ec15..d8092997e27 100644
--- a/dlls/amdxc64/main.c
+++ b/dlls/amdxc64/main.c
@@ -389,7 +389,7 @@ HRESULT STDMETHODCALLTYPE AmdExtD3DFactory_CreateInterface(IAmdExtD3DFactory *if
 
 HRESULT STDMETHODCALLTYPE AmdExtD3DFactory_QueryInterface(IAmdExtD3DFactory *iface, REFIID iid, void **out)
 {
-    TRACE("%p %s %p", iface, debugstr_guid(iid), out);
+    TRACE("%p %s %p\n", iface, debugstr_guid(iid), out);
     return E_NOINTERFACE;
 }
 
@@ -430,3 +430,23 @@ HMODULE WINAPI AmdGetDxcModuleHandle(void)
 {
     return GetModuleHandleA(NULL);
 }
+
+BOOL WINAPI DllMain(HINSTANCE inst, DWORD reason, void *reserved)
+{
+    const char *env;
+
+    switch (reason)
+    {
+        case DLL_PROCESS_ATTACH:
+        {
+            if ((env = getenv("FSR4_WATERMARK")) && !strcmp(env, "1"))
+            {
+                _putenv("MLSR-WATERMARK=1");
+            }
+            break;
+        }
+        default: break;
+    }
+
+    return TRUE;
+}
-- 
2.54.0

