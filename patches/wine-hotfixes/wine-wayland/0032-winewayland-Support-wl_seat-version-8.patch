From cb6c40a1824578d8ed4ce4f0f99b0c4a43f6d8cd Mon Sep 17 00:00:00 2001
From: Ashley Hauck <22095-khyperia@users.noreply.gitlab.winehq.org>
Date: Thu, 1 Jan 2026 16:15:28 +0100
Subject: [PATCH 032/239] winewayland: Support wl_seat version 8.

---
 dlls/winewayland.drv/wayland.c         |  2 +-
 dlls/winewayland.drv/wayland_pointer.c | 19 +++++++++++++------
 2 files changed, 14 insertions(+), 7 deletions(-)

diff --git a/dlls/winewayland.drv/wayland.c b/dlls/winewayland.drv/wayland.c
index 3ec39b313cb..fd774d9b311 100644
--- a/dlls/winewayland.drv/wayland.c
+++ b/dlls/winewayland.drv/wayland.c
@@ -139,7 +139,7 @@ static void registry_handle_global(void *data, struct wl_registry *registry,
         }
         pthread_mutex_lock(&seat->mutex);
         seat->wl_seat = wl_registry_bind(registry, id, &wl_seat_interface,
-                                         version < 5 ? version : 5);
+                                         version < 8 ? version : 8);
         seat->global_id = id;
         wl_seat_add_listener(seat->wl_seat, &seat_listener, NULL);
         pthread_mutex_unlock(&seat->mutex);
diff --git a/dlls/winewayland.drv/wayland_pointer.c b/dlls/winewayland.drv/wayland_pointer.c
index 237869eb1a2..489b5d8a1e1 100644
--- a/dlls/winewayland.drv/wayland_pointer.c
+++ b/dlls/winewayland.drv/wayland_pointer.c
@@ -288,8 +288,8 @@ static void pointer_handle_axis_stop(void *data, struct wl_pointer *wl_pointer,
 {
 }
 
-static void pointer_handle_axis_discrete(void *data, struct wl_pointer *wl_pointer,
-                                         uint32_t axis, int32_t discrete)
+static void pointer_handle_axis_value120(void *data, struct wl_pointer *wl_pointer,
+                                         uint32_t axis, int32_t value120)
 {
     INPUT input = {0};
     HWND hwnd;
@@ -302,20 +302,26 @@ static void pointer_handle_axis_discrete(void *data, struct wl_pointer *wl_point
     {
     case WL_POINTER_AXIS_VERTICAL_SCROLL:
         input.mi.dwFlags = MOUSEEVENTF_WHEEL;
-        input.mi.mouseData = -WHEEL_DELTA * discrete;
+        input.mi.mouseData = -value120;
         break;
     case WL_POINTER_AXIS_HORIZONTAL_SCROLL:
         input.mi.dwFlags = MOUSEEVENTF_HWHEEL;
-        input.mi.mouseData = WHEEL_DELTA * discrete;
+        input.mi.mouseData = value120;
         break;
     default: break;
     }
 
-    TRACE("hwnd=%p axis=%u discrete=%d\n", hwnd, axis, discrete);
+    TRACE("hwnd=%p axis=%u value120=%d\n", hwnd, axis, value120);
 
     NtUserSendHardwareInput(hwnd, 0, &input, 0);
 }
 
+static void pointer_handle_axis_discrete(void *data, struct wl_pointer *wl_pointer,
+                                         uint32_t axis, int32_t discrete)
+{
+    pointer_handle_axis_value120(data, wl_pointer, axis, WHEEL_DELTA * discrete);
+}
+
 static const struct wl_pointer_listener pointer_listener =
 {
     pointer_handle_enter,
@@ -326,7 +332,8 @@ static const struct wl_pointer_listener pointer_listener =
     pointer_handle_frame,
     pointer_handle_axis_source,
     pointer_handle_axis_stop,
-    pointer_handle_axis_discrete
+    pointer_handle_axis_discrete,
+    pointer_handle_axis_value120
 };
 
 /**********************************************************************
-- 
2.55.0

