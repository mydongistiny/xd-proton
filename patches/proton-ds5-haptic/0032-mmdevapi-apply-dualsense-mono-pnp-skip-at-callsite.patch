From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: GE-Proton <git@localhost>
Date: Tue, 7 Jul 2026 00:00:02 -0600
Subject: [PATCH] mmdevapi: Apply DualSense mono PnP skip at registration.

Only the DualSense mono speaker should avoid fake PnP AudioEndpoint exposure.
Other render and capture endpoints still register exactly as before.
---
 dlls/mmdevapi/devenum.c | 3 ++-
 1 file changed, 2 insertions(+), 1 deletion(-)

diff --git a/dlls/mmdevapi/devenum.c b/dlls/mmdevapi/devenum.c
index 83c75ce58f3..4d5bff2b653 100644
--- a/dlls/mmdevapi/devenum.c
+++ b/dlls/mmdevapi/devenum.c
@@ -1101,7 +1101,8 @@ static MMDevice *MMDevice_Create(const WCHAR *name, GUID *id, EDataFlow flow, DW
 
     MMDevice_Register(guidstr, friendly_name, device_iname,
         device_path.vt == VT_LPWSTR ? device_path.pwszVal : NULL,
-        container_id.vt == VT_CLSID ? container_id.puuid : &GUID_NULL, flow);
+        container_id.vt == VT_CLSID ? container_id.puuid : &GUID_NULL,
+        dualsense_mono_endpoint, flow);
     PropVariantClear(&device_path);
     PropVariantClear(&container_id);
 
-- 
2.54.0
